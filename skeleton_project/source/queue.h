#include <stdio.h>
#include <stdlib.h>
#include "hardware.h"

#DEFINE 

int width = 5;
int height = 5;
int** queueMatrix = malloc int*[width];
for(int i = 0; i < width; ++i){
   queueMatrix[i] = malloc int[height];
}

class Queue{
    private:
        int** queueMatrix;
    public:
        void updateQueue()
        Queue();
    
}
